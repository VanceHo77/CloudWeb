export class DropdownObject {
    category: string;
    title: string;
    uri: string;

    constructor(category, title, uri) {
        this.category = category;
        this.title = title;
        this.uri = uri;
    }
}